#pragma once
#include <string>
#include <vector>

namespace series {

    // Función principal para crecar series del string 
    std::vector<std::string> slice(const std::string& digits, int n);

}  // namespace series
